<%= form_with(model: sample, local: true) do |form| %>
  <% if sample.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(sample.errors.count, "error") %> prohibited this sample from being saved:</h2>

      <ul>
      <% sample.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :examination_id %>
    <%= form.text_field :examination_id, id: :sample_examination_id %>
  </div>

  <div class="field">
    <%= form.label :title %>
    <%= form.text_field :title, id: :sample_title %>
  </div>

  <div class="field">
    <%= form.label :datetime %>
    <%= form.text_field :datetime, id: :sample_datetime %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
